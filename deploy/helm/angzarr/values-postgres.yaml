# PostgreSQL storage values for angzarr
# This is now the default storage backend

# PostgreSQL storage
storage:
  type: postgres
  postgres:
    uri: "postgres://angzarr:angzarr@angzarr-postgresql:5432/angzarr"

postgresql:
  enabled: true
  auth:
    postgresPassword: "postgres"
    username: "angzarr"
    password: "angzarr"
    database: "angzarr"
  primary:
    persistence:
      size: 2Gi
    resources:
      requests:
        memory: "128Mi"
        cpu: "50m"
      limits:
        memory: "512Mi"
        cpu: "500m"
