apiVersion: v2
name: angzarr-bus-nats
description: NATS for Angzarr event bus (Bitnami wrapper)
type: application
version: 0.1.0
appVersion: "2.10"

dependencies:
  - name: nats
    version: "~8.0"
    repository: "https://charts.bitnami.com/bitnami"
