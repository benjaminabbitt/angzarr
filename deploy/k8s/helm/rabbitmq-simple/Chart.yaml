apiVersion: v2
name: angzarr-mq-rabbitmq-simple
description: Simple RabbitMQ deployment for CI (no operator required)
type: application
version: 0.1.0
appVersion: "4.1"
keywords:
  - rabbitmq
  - amqp
  - message-queue
  - angzarr
  - ci
maintainers:
  - name: Benjamin Abbitt

# No dependencies - deploys a simple StatefulSet directly
# Does NOT require RabbitMQ Cluster Operator
# Use for CI environments where operator installation is undesirable
