NATS deployed successfully!

Image: {{ .Values.nats.container.image.repository }}:{{ .Values.nats.container.image.tag }}
JetStream: {{ if .Values.nats.config.jetstream.enabled }}enabled{{ else }}disabled{{ end }}

Services created:
  - {{ .Release.Name }}-nats  (NATS: 4222, Monitor: 8222)

Connection string for Angzarr:
  nats://{{ .Release.Name }}-nats.{{ .Release.Namespace }}.svc.cluster.local:4222

Check status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

View logs:
  kubectl logs -l app.kubernetes.io/name=nats,app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

Use nats-box for debugging:
  kubectl exec -it deployment/{{ .Release.Name }}-nats-box -n {{ .Release.Namespace }} -- sh
  # Inside nats-box:
  nats stream ls
  nats pub test "hello world"
  nats sub test

Monitor endpoint:
  kubectl port-forward svc/{{ .Release.Name }}-nats 8222:8222 -n {{ .Release.Namespace }}
  Then open: http://localhost:8222/varz
