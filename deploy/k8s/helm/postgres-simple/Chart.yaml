apiVersion: v2
name: angzarr-db-postgres-simple
description: Simple PostgreSQL deployment for CI (no operator required)
type: application
version: 0.1.0
appVersion: "16"
keywords:
  - postgresql
  - postgres
  - database
  - angzarr
  - ci
maintainers:
  - name: Benjamin Abbitt

# No dependencies - deploys a simple StatefulSet directly
# Does NOT require CloudNativePG operator
# Use for CI environments where operator installation is undesirable
