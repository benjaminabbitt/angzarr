Angzarr Operators installed successfully!

Operators installed via Helm:
{{- if .Values.cloudnativepg.enabled }}
  ✓ CloudNativePG (PostgreSQL) - watching all namespaces
{{- end }}
{{- if .Values.strimzi.enabled }}
  ✓ Strimzi (Kafka) - watching all namespaces
{{- end }}

Additional operators to install manually:

  RabbitMQ Cluster Operator:
    kubectl apply -f https://github.com/rabbitmq/cluster-operator/releases/download/v{{ .Values.rabbitmq.version }}/cluster-operator.yml

  Or use: just operators-rabbitmq

Next steps:
  1. Install RabbitMQ operator (see above)
  2. Deploy infrastructure to angzarr namespace:
     helm install angzarr-db ./deploy/helm/angzarr-db-postgres -n angzarr
     helm install angzarr-mq ./deploy/helm/angzarr-mq-rabbitmq -n angzarr
     helm install angzarr-kafka ./deploy/helm/angzarr-mq-kafka -n angzarr

Verify operators are running:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get pods -n rabbitmq-system
