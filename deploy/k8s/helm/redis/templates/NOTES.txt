Redis deployed successfully!

Instance: {{ include "angzarr-db-redis.fullname" . }}
Architecture: {{ .Values.architecture }}
Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

Services created:
  - {{ include "angzarr-db-redis.fullname" . }}  (Redis: {{ .Values.service.port }})

Connection string for Angzarr:
  redis://{{ if .Values.auth.enabled }}:<password>@{{ end }}{{ include "angzarr-db-redis.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Check status:
  kubectl get statefulset {{ include "angzarr-db-redis.fullname" . }} -n {{ .Release.Namespace }}
  kubectl get pods -l app.kubernetes.io/name={{ include "angzarr-db-redis.name" . }} -n {{ .Release.Namespace }}

View logs:
  kubectl logs -l app.kubernetes.io/name={{ include "angzarr-db-redis.name" . }} -n {{ .Release.Namespace }}

Connect via redis-cli:
  kubectl exec -it {{ include "angzarr-db-redis.fullname" . }}-0 -n {{ .Release.Namespace }} -- redis-cli{{ if .Values.auth.enabled }} -a $REDIS_PASSWORD{{ end }}

Get password:
  kubectl get secret {{ include "angzarr-db-redis.secretName" . }} -n {{ .Release.Namespace }} -o jsonpath='{.data.redis-password}' | base64 -d
