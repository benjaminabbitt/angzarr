apiVersion: v2
name: angzarr-db-redis
description: Redis for Angzarr snapshot store (official image)
type: application
version: 0.2.0
appVersion: "7.4"
keywords:
  - redis
  - cache
  - snapshot
  - angzarr
  - event-sourcing
maintainers:
  - name: Benjamin Abbitt

# No dependencies - uses official redis image directly
# Simple StatefulSet deployment (no operator required)
