apiVersion: v2
name: angzarr-db-postgres
description: PostgreSQL storage backend for Angzarr (CloudNativePG)
type: application
version: 0.2.0
appVersion: "16"
keywords:
  - postgresql
  - postgres
  - database
  - angzarr
  - event-sourcing
  - cloudnativepg
maintainers:
  - name: Benjamin Abbitt

# No dependencies - deploys CloudNativePG Cluster CR directly
# Requires: CloudNativePG operator installed in cluster
# Install operator: helm install angzarr-operators ./deploy/helm/angzarr-operators -n operators
