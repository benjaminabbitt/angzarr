apiVersion: v2
name: angzarr-mq-rabbitmq
description: RabbitMQ message bus for Angzarr (RabbitMQ Cluster Operator)
type: application
version: 0.2.0
appVersion: "4.1"
keywords:
  - rabbitmq
  - amqp
  - message-queue
  - angzarr
  - event-sourcing
maintainers:
  - name: Benjamin Abbitt

# No dependencies - deploys RabbitmqCluster CR directly
# Requires: RabbitMQ Cluster Operator installed in cluster
# Install operator: kubectl apply -f https://github.com/rabbitmq/cluster-operator/releases/latest/download/cluster-operator.yml
# Or: just operators-rabbitmq
