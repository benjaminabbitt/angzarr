[workspace]
members = [
    ".",
    "crates/trivial-delegation",
    "xtask",
]
# sererr is a submodule with its own cargo workspace. Cargo's auto-discovery
# would otherwise treat sererr/packages/rust/* as members of THIS workspace.
# `exclude` removes them from membership but does NOT stop cargo's path-dep
# `.workspace = true` inheritance lookup from walking up and claiming this
# workspace as the root (a known cargo limitation around nested workspaces).
# The `[workspace.package]` block below mirrors sererr's own at
# sererr/packages/rust/Cargo.toml so that the inheritance resolves to the
# right values even when claimed by the wrong workspace. Sync if sererr's
# upstream values change.
exclude = ["sererr"]
resolver = "2"

# Mirrored from sererr/packages/rust/Cargo.toml's [workspace.package].
# Only consumed by sererr's path-dep inner crates' `<field>.workspace = true`
# inheritance. Has no effect on the angzarr crate itself (which sets its
# package fields explicitly in [package] below).
[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["sererr contributors"]
license = "MIT OR BSD-3-Clause"
homepage = "https://sererr.fyi"
repository = "https://github.com/sererr/sererr"
keywords = ["error", "stack-trace", "sentry", "backtrace", "diagnostics"]
categories = ["development-tools::debugging"]

# Mirrored from sererr/packages/rust/Cargo.toml's [workspace.dependencies].
# Consumed only by sererr's path-dep inner crates when they declare
# `<dep> = { workspace = true }`. Paths are rewritten to be relative to
# THIS Cargo.toml (sererr/packages/rust/<crate>) because cargo resolves
# them against the claiming workspace root.
[workspace.dependencies]
backtrace = "0.3"
prost = "0.14"
sererr = { version = "0.1", path = "sererr/packages/rust/sererr" }
sererr-proto = { version = "0.1", path = "sererr/packages/rust/sererr-proto" }

[package]
name = "angzarr"
version = "0.5.1"
edition = "2021"
authors = ["Benjamin Abbitt"]
description = "Schema-first CQRS/ES framework for polyglot client logic"
license = "AGPL-3.0-only"

[features]
# Default: otel for observability
default = ["otel"]

# =============================================================================
# Deployment Profiles (opinionated stacks)
# =============================================================================
# Note: sqlite is always compiled (no feature gate) for local orchestration support
# k8s: Generic Kubernetes with Postgres + RabbitMQ
k8s = ["postgres", "amqp", "otel", "dep:kube", "dep:k8s-openapi"]
# gcp-cloudrun: GCP serverless with Cloud SQL + Pub/Sub + GCS
gcp-cloudrun = ["postgres", "pubsub", "gcs", "otel"]
# gcp-k8s: GCP Kubernetes with Bigtable + Pub/Sub + GCS
gcp-k8s = ["bigtable", "pubsub", "gcs", "otel"]
# aws-k8s: AWS Kubernetes with DynamoDB + SNS/SQS + S3
aws-k8s = ["dynamo", "sns-sqs", "s3", "otel"]

# =============================================================================
# Messaging Backends
# =============================================================================
amqp = ["dep:lapin", "dep:deadpool-lapin"]
kafka = ["dep:rdkafka"]
pubsub = ["dep:gcloud-pubsub", "dep:gcloud-googleapis"]
sns-sqs = ["dep:aws-sdk-sns", "dep:aws-sdk-sqs", "dep:aws-config"]

# =============================================================================
# Storage Backends
# =============================================================================
# Note: sqlx/sea-query are always compiled with sqlite; these features add additional backends
postgres = ["sqlx/postgres", "sqlx/uuid", "sea-query-binder/sqlx-postgres"]
# sqlite is always enabled (no feature gate) - required for local orchestration
redis = ["dep:redis"]
immudb = ["sqlx/postgres", "sqlx/uuid"]  # Uses pgsql wire protocol
bigtable = ["dep:bigtable_rs"]
dynamo = ["dep:aws-sdk-dynamodb", "dep:aws-config"]

# =============================================================================
# Payload Store Backends (claim check pattern)
# =============================================================================
gcs = ["dep:gcloud-storage"]
s3 = ["dep:aws-sdk-s3", "dep:aws-config"]

# =============================================================================
# Observability & Utilities
# =============================================================================
otel = ["dep:opentelemetry", "dep:opentelemetry_sdk", "dep:opentelemetry-otlp", "dep:opentelemetry-appender-tracing", "dep:tracing-opentelemetry"]
lossy = ["dep:rand"]
# Test utilities for integration tests
test-utils = []
# Enable #[coverage(off)] attribute on nightly (for trivial_delegation)
coverage_nightly = []

# =============================================================================
# All Features (for CI/development)
# =============================================================================
# Note: sqlite is always compiled, not listed here
full = ["k8s", "gcp-cloudrun", "gcp-k8s", "aws-k8s", "kafka", "redis", "immudb", "lossy"]
# full-musl: For musl static builds - excludes kafka (requires librdkafka with OpenSSL)
# GCP crates use rustls-tls feature for musl compatibility
full-musl = ["k8s", "gcp-cloudrun", "gcp-k8s", "aws-k8s", "redis", "immudb", "lossy"]

[dependencies]
# gRPC
tonic = { version = "0.14", features = ["tls-ring", "transport"] }
tonic-health = "0.14"
tonic-reflection = "0.14"
prost = "0.14"
prost-types = "0.14"
tonic-prost = "0.14.5"
tokio-stream = { version = "0.1", features = ["net"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "cors"] }
http = "1"
http-body = "1"
bytes = "1"
hyper-util = { version = "0.1", features = ["tokio"] }

# Async runtime
tokio = { version = "1", features = ["full"] }

# Storage - Query builder approach (like jOOQ)
# SQLite is always compiled for local orchestration support
sea-query = "0.32"
sea-query-binder = { version = "0.7", features = ["sqlx-sqlite"] }
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "migrate"] }

# Retry with backoff
backon = "1"

# HTTP client for external service calls
# Using rustls-tls for musl static build compatibility (no OpenSSL dependency)
# ring provides the crypto backend for rustls
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }

# Core utilities
uuid = { version = "1", features = ["v4", "v5"] }
async-trait = "0.1"
thiserror = "2"
futures = "0.3"
chrono = "0.4"
hex = "0.4"
serde_json = "1"
inventory = "0.3"

# Protobuf reflection (for logging projector)
prost-reflect = { version = "0.16", features = ["serde"] }

# Logging/Tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# OpenTelemetry (optional, feature-gated)
opentelemetry = { version = "0.31", optional = true }
opentelemetry_sdk = { version = "0.31", features = ["rt-tokio"], optional = true }
opentelemetry-otlp = { version = "0.31", features = ["grpc-tonic", "metrics", "logs"], optional = true }
opentelemetry-appender-tracing = { version = "0.31", optional = true }
tracing-opentelemetry = { version = "0.32", optional = true }

# Configuration
config = "0.14"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
base64 = "0.22"

# AMQP/RabbitMQ (optional)
lapin = { version = "2", optional = true }
deadpool-lapin = { version = "0.12", optional = true }

# Redis (optional)
redis = { version = "0.27", features = ["aio", "connection-manager", "tokio-comp"], optional = true }

# Kafka (optional)
rdkafka = { version = "0.36", features = ["cmake-build", "ssl", "sasl"], optional = true }

# Google Pub/Sub (optional)
# gcloud-pubsub is yoshidan's crate (was google-cloud-pubsub before name donation to Google)
gcloud-pubsub = { version = "1.6", optional = true }
gcloud-googleapis = { version = "1.3", features = ["pubsub"], optional = true }

# AWS SNS/SQS (optional)
aws-sdk-sns = { version = "1", optional = true }
aws-sdk-sqs = { version = "1", optional = true }
aws-config = { version = "1", optional = true }

# AWS S3 (optional, for payload store)
aws-sdk-s3 = { version = "1", optional = true }

# AWS DynamoDB (optional, for event store)
aws-sdk-dynamodb = { version = "1", optional = true }

# Google Cloud Storage (optional, for payload store)
# gcloud-storage is yoshidan's crate (was google-cloud-storage before name donation to Google)
gcloud-storage = { version = "1.2", optional = true }

# Google Cloud Bigtable (optional, for event store)
bigtable_rs = { version = "0.2", optional = true }

# Random (for lossy testing bus)
rand = { version = "0.9", optional = true }

# Kubernetes API (for gateway service discovery) - optional, k8s feature only
kube = { version = "0.98", features = ["runtime", "client"], optional = true }
k8s-openapi = { version = "0.24", features = ["v1_29"], optional = true }

sha2 = "0.10.9"
mutants = "0.0.4"
trivial-delegation = { path = "crates/trivial-delegation" }

# Structured stack-trace + error-chain capture.
#   - `sererr` provides plain Rust types (no proto dep) used as the
#     in-process representation in `src/dlq/mod.rs`.
#   - `sererr-proto` provides the prost-generated `sererr.v1.*` types
#     referenced from `types.proto` via `import "sererr/sererr.proto"`.
#     angzarr's build.rs uses `extern_path` to wire the generated
#     `EventProcessingFailedDetails.stack_trace` field to
#     `sererr_proto::sererr_v1::CapturedError` instead of generating
#     a duplicate definition.
sererr = { path = "sererr/packages/rust/sererr" }
sererr-proto = { path = "sererr/packages/rust/sererr-proto" }

# Unix process management (signals, child-process lifecycle).
[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["signal", "process", "fs", "user"] }

[lints.rust]
# coverage_nightly is a custom cfg used for conditional #[coverage(off)] on nightly
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }

[build-dependencies]
tonic-prost-build = "0.14"
prost-build = "0.14"
prost = "0.14"
prost-types = "0.14"

[[bin]]
name = "angzarr-aggregate"
path = "src/bin/angzarr_aggregate.rs"

[[bin]]
name = "angzarr-projector"
path = "src/bin/angzarr_projector.rs"

[[bin]]
name = "angzarr-saga"
path = "src/bin/angzarr_saga.rs"

[[bin]]
name = "angzarr-process-manager"
path = "src/bin/angzarr_process_manager.rs"

[[bin]]
name = "angzarr-upcaster"
path = "src/bin/angzarr_upcaster.rs"

[[bin]]
name = "angzarr-status"
path = "src/bin/angzarr_status.rs"

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3"
cucumber = "0.21"
futures = "0.3"
serial_test = "3"
testcontainers = "0.27"
hex = "0.4"
rand = "0.9"

# =============================================================================
# Contract Test Binaries
# =============================================================================
# These tests verify that storage and bus implementations correctly fulfill
# their trait contracts. Each backend gets its own test binary with the
# required feature flag, ensuring:
#   1. Tests only compile when the backend is available
#   2. Clear error messages when running without required features
#   3. Isolated test runs per backend for CI parallelization
#
# Storage tests verify: EventStore, SnapshotStore, PositionStore traits
# Bus tests verify: EventBus trait (publish, subscribe, ack/nack, DLQ)
# =============================================================================

# --- Storage Contract Tests (Macro-based) ---
# All storage backends use macro-based tests for consistency and reduced overhead.
# Tests are organized by trait (EventStore, SnapshotStore, PositionStore) with
# reusable test suites in tests/storage/*.rs.

[[test]]
name = "storage_redis"
# Redis provides high-performance caching for snapshots. Testing ensures
# snapshot store/restore works correctly with Redis's eventual consistency model.
required-features = ["redis"]

[[test]]
name = "storage_immudb"
# ImmuDB provides cryptographically verifiable event storage. Testing ensures
# events persist correctly via the PostgreSQL wire protocol adapter.
required-features = ["immudb"]

[[test]]
name = "storage_sqlite"
# SQLite provides embedded storage for local-dev mode. Testing ensures all three
# stores (Event, Snapshot, Position) work correctly with in-memory SQLite.
# Note: sqlite is always compiled, no required-features needed

[[test]]
name = "storage_postgres"
# PostgreSQL provides production-grade storage. Testing ensures all three stores
# work correctly with full SQL support including historical snapshot queries.
required-features = ["postgres"]

# --- Bus Contract Tests ---

[[test]]
name = "bus_amqp"
# RabbitMQ/AMQP as event bus. Testing ensures exchange/queue bindings,
# message routing, acknowledgment, and dead-letter queue handling work correctly.
required-features = ["amqp", "test-utils"]

[[test]]
name = "bus_kafka"
# Apache Kafka as event bus. Testing ensures topic partitioning, consumer groups,
# offset management, and at-least-once delivery semantics work correctly.
required-features = ["kafka", "test-utils"]

[[test]]
name = "bus_pubsub"
# Google Cloud Pub/Sub as event bus. Testing ensures topic/subscription management,
# message ordering, and acknowledgment deadlines work correctly with the emulator.
required-features = ["pubsub", "test-utils"]

[[test]]
name = "bus_sns_sqs"
# AWS SNS/SQS as event bus. Testing ensures SNS fan-out to SQS queues,
# message attributes, and visibility timeout handling work correctly with LocalStack.
required-features = ["sns-sqs", "test-utils"]

# --- DLQ Gherkin acceptance harness ---
# Cucumber-rs harness against features/client/dlq.feature. `harness = false`
# hands main() control to cucumber's runner so the standard rust test
# harness doesn't intercept. Runs the saga/PM/projector scenarios via
# public orchestrate_saga / orchestrate_pm / ProjectorEventHandler::handle,
# and the aggregate scenario via publish_aggregate_sequence_mismatch_dlq.
# Each scenario gets a fresh tempfile SQLite DLQ publisher+reader pair.
# features/operator/dlq_boot.feature stays out of scope (needs bin spawning;
# init_dlq_publisher/init_dlq_reader unit tests pin those contracts).
[[test]]
name = "dlq_features"
harness = false
required-features = ["test-utils"]

# --- Deployed-cluster acceptance harness (T14) ---
# Cucumber-rs harness against features/acceptance/end_to_end.feature, driving
# a DEPLOYED angzarr system (kind + skaffold poker examples) through the
# public gRPC surface via the tests/common fixture layer. Opt-in: exits 0
# unless ANGZARR_ACCEPTANCE=1 so plain `cargo test` needs no cluster. The
# compensation_* features still need harness coverage (saga topology).
[[test]]
name = "acceptance_features"
harness = false
required-features = ["test-utils"]

# --- B1 e2e: persisted events ALWAYS reach the bus ---
# Real aggregate pipeline + real SQLite store + real RabbitMQ. Pins the
# persisted-but-never-published class (the 2026-04-07 full-sail interleave
# bug, fixed in c2fa2beb) with deterministic publish-failure injection and
# the original fact/command race shape.
[[test]]
name = "interleave_publish_amqp"
required-features = ["amqp", "test-utils"]

# --- Aggregate pipeline -> EventStore integration ---
# Runs the aggregate command pipeline (AggregateService.handle_command)
# through a real in-memory SqliteEventStore. Catches drift between the
# pipeline's persist logic and the production storage backend that
# MockEventStore-based unit tests can't see. Closes the highest-leverage
# Category B drift gap from the R2-15 audit.
[[test]]
name = "aggregate_pipeline_event_store"
required-features = ["test-utils"]

# --- PM persist -> EventStore integration ---
# Runs persist_pm_event_book (extracted from
# GrpcPMContext::persist_pm_events) against a real in-memory
# SqliteEventStore + a recording EventBus. PM events are the part of
# the framework that writes directly to storage without going through
# the aggregate command pipeline; PmWithEvents in unit tests stubs the
# outcome. Closes the second Category B drift gap from the R2-15 audit.
[[test]]
name = "pm_persist_event_store"
required-features = ["test-utils"]

# --- Status DLQ admin gRPC -> Reader -> DB integration ---
# Drives DlqAdminHandler's gRPC trait methods (list_dead_letters,
# get_dead_letter, delete_dead_letter) against a real SqliteDlqReader
# backed by a populated database. Closes the third Category B drift
# gap from the R2-15 audit -- the operator-facing publisher -> reader
# -> admin gRPC loop end-to-end.
[[test]]
name = "status_dlq_admin_grpc"
required-features = ["test-utils"]

# --- Aggregate pipeline -> SnapshotRepository integration ---
# Drives AggregateService.handle_command with a real SqliteSnapshotStore
# wired into SnapshotRepository. Catches snapshot-write policy bugs and
# pipeline-read-snapshot drift that MockSnapshotStore can't see.
[[test]]
name = "aggregate_pipeline_snapshot"
required-features = ["test-utils"]

# --- Projector publish -> EventBus.publish shape verification ---
# Closes the deep-shape-verification gap on the projector's publish
# path. Real-broker serialization is owned by bus_*.rs testcontainer
# suites; the unit tests only check publish_count. This test inspects
# the published EventBook (domain prefix, correlation_id, edition,
# decoded projection payload, sequence) via MockEventBus.
[[test]]
name = "projector_publish_event_bus"
required-features = ["test-utils"]

# === Build Profiles ===

# Dev profile - optimized for size to keep disk usage sane
[profile.dev]
opt-level = "z"
debug = 0
strip = true
incremental = true

# Container dev profile - optimized for size, no incremental
# Usage: cargo build --profile container-dev
[profile.container-dev]
inherits = "dev"
incremental = false

# Test profile - optimized for size
# Usage: cargo test (automatically used)
[profile.test]
opt-level = "z"
debug = 0
strip = true
incremental = true

# Release profile - slow compilation, fast runtime (for production)
[profile.release]
opt-level = "z"     # Optimize for size
lto = "fat"         # Maximum link-time optimization
codegen-units = 1   # Single codegen unit for best optimization
strip = true        # Strip symbols for smaller binaries
panic = "abort"     # Abort on panic (smaller binaries, no unwinding)

# Production profile - maximum optimization with all features
# Usage: cargo build --profile production --features full
[profile.production]
inherits = "release"
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
opt-level = 3

