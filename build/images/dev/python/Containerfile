# Python dev container for angzarr development
# ARG is set by skaffold's requires.alias

ARG PYTHON_IMAGE
FROM ${PYTHON_IMAGE}

# Mark as devcontainer for justfile detection
ENV DEVCONTAINER=true

WORKDIR /workspace
