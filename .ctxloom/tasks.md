# Tasks

<!-- ctxloom-tasks:v1 -->

## In Progress

- [ ] `immovable-undone-impulse` [HIGH][config] Storage config → central tagged enum. Replace StorageConfig flat storage_type + always-present parallel postgres/sqlite/redis/bigtable/dynamo/nats structs (storage/config.rs) AND the inventory self-registration (storage/factory.rs) with one internally-tagged serde enum — each variant carries only its backend config — resolved by a central match → Arc<dyn EventStore>/SnapshotStore/PositionStore. Single-entry: omit the tag when only one variant present. Encode the Redis=snapshots-only / ImmuDB=events-only constraints in the types, not hardcoded in the factory. TDD + cross-backend Cucumber harness (testcontainers). ✅DECIDED: split selections — separate EventStoreBackend / SnapshotStoreBackend / PositionStoreBackend tagged enums (role membership mirrors trait impls; redis snapshot-only, immudb event-only). PROGRESS: REGISTRY CONFIG MODEL done in storage/config.rs (additive, #[allow(dead_code)], build green): BackendConfig tagged union (postgres/sqlite/redis/[immudb]/[bigtable]/[dynamo]/composite) + dedicated ImmudbConfig + CompositeBackendConfig{main,editions} + BackendRef{use} + StorageRegistryConfig{backends map, events/snapshots/positions refs, snapshots_enable} + StorageRole + validate() enforcing role-capability and composite main(event-capable)/editions(reclaim-capable) refs. 12 config tests green (deser full registry, validate ok, reject redis-as-event, unknown ref, composite validates, composite rejects non-reclaiming editions, default validates). COMMITTED (4 commits b1eb2416, a07cb9ea, d49e463e, d0e2fe12): (a) registry config model + resolve()/validate() + tests; (b) registry factory init_event_store/init_snapshot_store/init_position_store_registry; (c) SWAP DONE — Config.storage + server overrides now StorageRegistryConfig, bins use the new init fns, config.example.yaml + config_integration updated, registry is the LIVE path; (d) bigtable/dynamo wired (+ fixed pre-existing Cover.ext proto drift in their event stores). postgres/sqlite/redis/bigtable/dynamo all resolve via the registry; verified build default + --features postgres,redis + --features bigtable,dynamo; 988 lib + 14 config tests green. REMAINING: (1) immudb arm — still NotImplemented, pending its cascade-column rework (stainable-anguished-cuddle); (2) CompositeEventStore — composite arm still NotImplemented (namespace-partition stitch); (3) CLEANUP — delete the now-orphaned legacy path: old init_storage/init_position_store, StoresBackend/PositionBackend + inventory::collect!, CreateStoresFn/CreatePositionFn, the 8 inventory::submit! blocks in postgres/sqlite/bigtable/dynamo mods, the legacy flat StorageConfig + its test, and rename init_position_store_registry → init_position_store; (4) cross-backend behavioral Cucumber harness; (5) cargo test + just mutants. ✅DESIGN PIVOT (registry): define backends ONCE in a named connection/pool registry — `backends: { <name>: <tagged-union backend config> }` — then the role selections reference registry entries by name (`events: {use: <name>}`, `snapshots: {use: <name>}`, `positions: {use: <name>}`). The discriminated union (backend type + config) lives in the registry values; roles discriminate against it by reference. Removes the per-role connection repetition. Role-capability (redis = snapshot-only, immudb = event-only) becomes a validation check that the referenced entry supports the role (parse-time enum-variant rejection no longer applies once it's a ref). NOTE: the inline-config EventStoreBackend/SnapshotStoreBackend/PositionStoreBackend enums already added are a stepping stone — rework them to the registry+ref shape (registry value = one BackendConfig union; role = ref). Pairs with the conformance-audit task (which backends the registry should support) and the immudb-rework task (stainable-anguished-cuddle). ✅COMPOSITE backend: the registry must support a `composite` EventStore backend type whose config REFERENCES two other registry entries by name — `events: {use: {type: composite, main: <name>, editions: <name>}}`. It resolves to a generic `CompositeEventStore { main: Arc<dyn EventStore>, editions: Arc<dyn EventStore> }` (itself an EventStore, decorator like Instrumented/Offloading; NOT immudb-specific). Reference validation: `main` must be an append-only-capable event backend, `editions` must be delete-capable. NAMESPACE-PARTITION INVARIANT (load-bearing): the `main` store holds ONLY the default chain; the `editions` store holds ONLY non-default editions — so each underlying store reconstructs "its part" and the composite stitches main[0..divergence] + edition[divergence..] (divergence from the editions metadata table, schema.rs:90). Routing by the `edition` arg; delete_edition_events → editions store (reclaims space); MAIN_TIMELINE_PROTECTED guards the default chain. This is the mechanism enabling immudb (append-only main) + sql (mutable editions) per stainable-anguished-cuddle.
- [ ] `stubbly-cohesive-hug` [HIGH][feature][TDD] Persist & round-trip Cover.ext across ALL event-store backends. FINDING: Cover.ext (google.protobuf.Any — parent-aggregate routing cover, stamped on every emitted book) is NOT persisted by any backend — no schema column, add() doesn't even receive it (signature: domain,edition,root,events,correlation_id,external_id,source_info — no ext), and every read-reconstruction (helpers::assemble_event_books, repository/event_book, bigtable, dynamo) sets ext=None. So it's dropped on store/load everywhere (systemic latent data-loss). RED DONE: failing cross-backend contract test test_cover_ext_round_trips added to tests/storage/event_store_tests.rs (stores a book with ext via add(), asserts get_by_correlation reconstructs identical ext) — currently fails to compile (add takes 7 args). GREEN PLAN: (1) EventStore::add gains ext: Option<&prost_types::Any> (8th param) — trait + ALL impls (sql, mock, bigtable, dynamo, immudb) + the Instrumented<S> forwarding wrapper (advice/instrumented.rs) + every caller (repository/event_book:378 passes book.cover.ext; ~30 contract-suite calls pass None). (2) schema: add an ext BLOB/bytes column to events tables (migrations sqlite + postgres), a column qualifier (bigtable), an attribute (dynamo); store serialized Any per event row (mirrors correlation_id; design accepts per-event cost). (3) read reconstruction: helpers::assemble_event_books + sql get/get_by_correlation + bigtable/dynamo inline + mock populate cover.ext from the stored bytes. (4) wire test into each backend test file. VERIFY: sqlite + mock run locally (no container) — green there first; postgres/bigtable/dynamo/immudb via testcontainers/CI. Use TDD; behavioral contract is backend-agnostic. Replaces the ext:None placeholders (incl. the bigtable/dynamo ones from d0e2fe12).

## To Do

- [ ] `tacky-proven-fraction` [CRIT] PM correlation_id parsed as UUID → nil-UUID collision (orchestration/process_manager/mod.rs:675). Non-UUID correlation_ids (the normal case, e.g. "corr-1") all collapse to Uuid::nil(), breaking rejection/compensation routing. Covers the orch-misc dup + the unused-const cleanup. ✅DECISION: store the RAW correlation_id on the PM cover (carry the original string; needs a ProtoUuid/cover schema change) — do NOT derive a v5 hash. Preserves the "correlation_id IS the PM root" invariant. The now-unused ANGZARR_UUID_NAMESPACE (correlation.rs:19) can just be deleted.
- [ ] `unloving-viewable-backrest` [CRIT] DynamoDB event store: write condition attribute_not_exists(pk) is always false (pk is written into every item), so every put_item fails with ConditionalCheckFailedException — the backend cannot persist. Change to attribute_not_exists(seq). storage/dynamo/event_store.rs:431.
- [ ] `exemplary-smokeless-sedation` [CRIT] Mock PositionStore allows checkpoint regression — unconditional insert violates C-17 monotonic contract (storage/mock/position_store.rs:60). Enforce monotonic put (only advance) and run the position_store contract test against the mock (currently only run against sqlite/postgres/nats).
- [ ] `outspoken-next-common` [CRIT][DESIGN] Define the "subscribe to all domains" contract for the EventBus trait — currently undefined and each backend fails differently (kafka works via regex; sns_sqs/nats/pubsub broken). Prereq for the three per-backend fixes. ✅DECISION: REQUIRE explicit domain names — no implicit wildcard/discovery. Domain list is populated from an env var by the deployment (which knows the names). Ensure config reads domains from env (IPC already does this — load_subscribers_from_env at bus/ipc/client.rs:399; cf. ANGZARR_SUBSCRIPTIONS in CLAUDE.md); make the other backends do the same. Empty domains = explicit configuration error, NOT a synthetic/fallback subscription.
- [ ] `operable-latticed-motive` [CRIT] Fix subscribe-all in SNS/SQS bus: empty-domains creates a synthetic "events" domain no one publishes to (bus/sns_sqs/bus.rs:407), and the empty original filter (not the local list) is passed to the consumer (:436) so it matches everything. Conform to the subscribe-all contract task. ✅DECISION: require explicit domains (from env); remove the synthetic "events" fallback — empty domains is an error.
- [ ] `empty-crestless-hertz` [CRIT] Fix subscribe-all in Pub/Sub bus: empty-domains falls back to literal "events" topic (→ "angzarr-events-events") subscribing to nothing (bus/pubsub/bus.rs:166), and create_subscriber(name, None) inherits the break (:260). Conform to the subscribe-all contract task. ✅DECISION: require explicit domains (from env); remove the literal "events" fallback — empty domains is an error.
- [ ] `spiny-agreed-feel` [HIGH] Aggregate: enforce the command/fact deferred-sequence boundary. has_deferred_sequence and stamp_deferred_sequences accept both AngzarrDeferred (commands) and ExternalDeferred (facts) (orchestration/aggregate/parsing.rs:78,113); the command pipeline lacks the symmetric reject-validation facts have (pipeline.rs:154). Split the functions and reject ExternalDeferred markers on commands.
- [ ] `perky-chosen-dreamland` [HIGH] DynamoDB: add pagination (LastEvaluatedKey/exclusive_start_key) to all scan()s — query_stale_cascades (:952), list_roots (:561), list_domains (:596), delete_edition_events (:784), query_cascade_participants (:1018). Unpaginated 1MB-chunk scans silently truncate, e.g. CascadeReaper misses stale 2PC cascades. storage/dynamo/event_store.rs.
- [ ] `tidy-reliable-ambition` [HIGH] DynamoDB: seq parse failures silently unwrap_or(0), corrupting event ordering & idempotency — find_by_source (event_store.rs:877) and find_by_external_id (:937). ✅DECISION: return an error (not skip, not default-0) — missing/malformed required fields are errors.
- [ ] `palpable-sinless-umbrella` [HIGH] Bigtable: query_stale_cascades tracks staleness per cascade_id instead of per-participant, so one fresh participant marks the whole cascade not-stale (storage/bigtable/event_store.rs:1349). Key state by (cascade_id, domain, edition, root) per the trait contract.
- [ ] `shiftless-unmindful-spree` [HIGH] SQLite: source_info.seq (u32) cast to i32 without bounds in find_by_source query (storage/sqlite/event_store.rs:756) — values > i32::MAX wrap and mismatch. Use i32::try_from with a StorageError on failure.
- [ ] `revocable-empirical-dime` [HIGH] Empty-events bypass idempotency check — add([], external_id) returns Added instead of Duplicate in both postgres (event_store.rs:171) and sqlite (event_store.rs:421). Move the external_id idempotency check before the empty-events early return, per EventStore trait contract.
- [ ] `baffling-upturned-chaplain` [HIGH] ImmuDB EventStore: cascade tracking unimplemented (query_stale_cascades/query_cascade_participants return NotImplemented; schema lacks committed/cascade_id cols) → CascadeReaper can't process 2PC timeouts on ImmuDB (storage/immudb/event_store.rs:787, mod.rs:102). ✅DECISION: IMPLEMENT it (not exclude). Immutability is NOT a blocker — the cascade flow is append-only: pending events INSERTed no_commit=true, the reaper INSERTs a Revocation marker (no_commit=false, reaper.rs:175-191), and resolution is read-time filtering (two_phase.rs); there is no UPDATE/DELETE in the cascade path. So add committed/cascade_id columns to the immudb schema (mod.rs:102) and implement query_stale_cascades/query_cascade_participants as SELECTs. ⚠️VERIFY (done — low risk): immudb docs say subqueries are "not constrained" and correlated subqueries are supported (esp. FROM-clause); no documented NOT EXISTS restriction, so the cascade query is likely portable. Two real implementation constraints: (a) CONFIRMED — immudb's pgsql server is simple-query-mode ONLY (no extended query protocol / prepared statements; JDBC needs preferQueryMode=simple), so the existing raw_sql + interpolated/escaped-threshold approach (immudb/event_store.rs:9-10) is mandatory, not a style choice; (b) immudb's SQL dialect is NOT Postgres-compatible (own engine) and docs emphasize FROM-clause correlation — so smoke-test the WHERE-clause correlated NOT EXISTS against the immudb testcontainer (tests/storage_immudb) rather than assuming the postgres SQL ports verbatim. delete_edition_events stays NotImplemented — correct and unrelated to cascades.
- [ ] `slimy-unworn-gab` [HIGH] Sync projector failures are logged but execute() still returns Ok with empty/partial projections to the gRPC client (handlers/core/aggregate.rs:137) — client can't distinguish "no projections" from "failed". ✅DECISION: FAIL the command — return a Status error to the gRPC client when a sync projector fails (do not return Ok with partial/empty projections).
- [ ] `stuffy-unsafe-venue` [HIGH] DefaultEscalationHandler::new() calls .expect() on reqwest client build — panic reachable during saga compensation/event processing (utils/saga_compensation/mod.rs:151). Return Result and propagate, or build lazily via OnceCell.
- [ ] `docile-overnight-chant` [HIGH] Kafka calls crate::bus::dispatch_to_handlers (bus/kafka/bus.rs:198) while every other backend uses crate::bus::dispatch::dispatch_to_handlers — inconsistent/unexported path is fragile. Standardize the path or add an explicit pub use re-export in bus/mod.rs.
- [ ] `unstylish-viable-hemstitch` [HIGH][SECURITY] Path traversal in FilesystemPayloadStore.path_from_uri (payload_store/filesystem.rs:55) — a network-supplied URI like file:///../../etc/passwd is read with no base-path check. Canonicalize the resolved path and verify it starts_with base_path.
- [ ] `provable-early-feel` [HIGH] Two-phase replay audit timestamp mismatch — begin_pending and commit_audit each call Utc::now() separately, so pending vs final ReplayAuditRecord can differ (status/handlers/dlq.rs:432,570). Capture replayed_at once and pass it into commit_audit.
- [ ] `darkish-unisexual-appointee` [HIGH] AngzarrDeferredSequenceExt::idempotency_key() calls .expect("source required") — panic on malformed/untrusted proto with source=None (proto_ext/pages.rs:256). ✅DECISION: return an error (not a default) — missing required fields are errors. Best: also enforce source presence at the proto/validation layer so it can't reach this code. Same error-not-default rule applies to the Dynamo seq-parse task.
- [ ] `unblessed-flaky-spectrum` [HIGH] Integer overflow: get_sequences computes max_seq + 1 which wraps to 0 at u32::MAX (release mode) → backwards query bounds (repository/event_book/mod.rs:328). Use saturating_add and add a u32::MAX boundary test.
- [ ] `hardened-shiny-landfall` [HIGH] Aggregate sidecar documents embedded mode (spawn target.command, health-check, connect) but doesn't implement it; projector sidecar does (bin/angzarr_aggregate.rs:20,38 vs angzarr_projector.rs:113). ✅DECISION: embedded mode has been removed — just DELETE the stale docs (do not implement). Also check the projector sidecar for leftover embedded-mode code/docs and remove those too if present.
- [ ] `uncharted-unbent-passing` [HIGH] xtask: #[trivial_delegation] on a free function emits regex "::name" but cargo-mutants outputs bare "name" for free functions → exclude won't match (xtask/src/main.rs:128). Drop the :: prefix for free functions (latent; no free fns use it yet).
- [ ] `icky-gone-target` [MED][SECURITY] Harden untrusted-input handling: sanitize the domain used in DLQ filenames (dlq/publishers/filesystem.rs:74, offload.rs:81), validate socket path construction (config/server.rs:119), and validate correlation_id format at the PM gRPC entry point (orchestration/process_manager/mod.rs:376).
- [ ] `unlucky-amendable-stump` [MED] Checked integer conversions for DB values: replace unsafe signed↔unsigned casts in postgres (event_store.rs:205,227,474,503) and sqlite (event_store.rs:128,642,672) with try_from + error.
- [ ] `barbed-mossy-encounter` [MED] Stop silently swallowing store errors: sqlite ROLLBACK error swallowed (event_store.rs:469), redis corrupted-snapshot skip (snapshot_store.rs:114), dynamo malformed query_cascade_participants items skipped (event_store.rs:1018) — log/propagate instead of dropping.
- [ ] `wiry-marauding-yearling` [MED] Mock PositionStore concurrency: replace std sync RwLock held across .await and lock-poisoning unwraps with async lock + graceful handling (storage/mock/position_store.rs:4,48,60). (pairs with the critical monotonic-put fix.)
- [ ] `erasable-feeble-draw` [MED] Panic in shutdown signal-handler installation — bin/angzarr_saga.rs:257 (saga & process-manager) and utils/bootstrap.rs:228. Handle the error path instead of panicking.
- [ ] `footless-unworldly-appendix` [MED] gap_fill RemoteEventSource.get_from_to has a latent range-conversion bug for empty ranges (services/gap_fill/filler.rs:341).
- [ ] `unenvied-educated-object` [MED] Silent correlation-id data loss when gRPC metadata parsing fails in correlated_request() (proto_ext/grpc.rs:16) — surface/propagate the parse failure instead of dropping the correlation id.
- [ ] `mushy-unissued-smith` [MED] registration backoff jitter: poor entropy source and potential overflow with large attempt counts; inaccurate jitter-range comment (registration/mod.rs:113,122). Use a proper RNG + saturating math.
- [ ] `uneven-unbounded-dainty` [LOW][cleanup] Delete dead code (prototype mandate): registration RepublishStrategy & impls (registration/mod.rs:1-135, never used), unused RPC_DURATION metric (status/metrics.rs:45), unused fallback_edition() (storage/helpers/mod.rs:28), duplicate SQLite pool init (storage/sqlite/mod.rs:25).
- [ ] `stricken-chubby-triangle` [LOW][cleanup] De-duplicate repeated logic: cross-backend bus consumer loops (pubsub/sns_sqs/kafka/nats), advice LossyBus/LossyDynBus publish (advice/lossy.rs:193,277), instrumented_bus/instrumented_handlers feature-gating, utils/sidecar retry (sidecar.rs:94), handlers stream send_to_subscribers (stream/mod.rs:275).
- [ ] `scrawny-crinkly-railroad` [LOW][arch] proto_ext extension-trait violations: free functions in proto_ext/books.rs:33,42 should be trait methods; replace fragile string-match error handling in storage/nats/position_store.rs:92 with typed errors.
- [ ] `spendable-fiddling-fondness` [LOW] Remaining correctness nits: DLQ check-then-act races (dlq/publishers/pubsub.rs:106, sns_sqs.rs:120), transport/uds TOCTOU (transport/uds.rs:62), amqp misleading retry counter (amqp/mod.rs:703), immudb wrong blob-decode error type (immudb/event_store.rs:25), nats inconsistent cascade UUID formatting (nats/event_store.rs:816); plus comment/log fixes (saga phase numbering saga/mod.rs:512, discovery/k8s logs :527, dlq database_reader redundant conversion :139, xtask comment :111).
- [ ] `jubilant-obsessive-cure` [HIGH] Wire the payload-offload claim-check into production binaries. The infra is complete but DEAD in prod: OffloadingEventBus + PayloadReference (bus/offloading.rs), wrap_with_offloading (factory.rs:65), EventPage.payload oneof {event|external}, S3/GCS PayloadStores, PayloadOffloadConfig (config/mod.rs:111) — but init_payload_store()/wrap_with_offloading() are never called by angzarr_aggregate/saga/process_manager/projector, so large payloads are unsupported on ALL transports. Fix: call init_payload_store(config.payload_offload) + wrap_with_offloading(bus, store, threshold) right after init_event_bus() in each binary; enable via YAML/env. This is the generic resolution to the IPC oversized issue (see unstaffed-remote-excess). ✅REQUIREMENT (already satisfied by the config shape — just honor it): the claim-check has its OWN optional storage config, independent of the event store — PayloadOffloadConfig (payload_store/config.rs:28) → enabled:bool (optional switch; false = all inline, no store needed), store_type (its own filesystem/gcs/s3 selection), and per-backend blocks (filesystem/gcs/s3) + threshold_bytes/retention_hours/cleanup_interval_secs. Wiring must: respect enabled=false (skip init_payload_store entirely, no store required), and let the payload store differ from the event-store backend (e.g. events in Postgres, payloads in S3). Do NOT couple the offload store to the event-store config. ✅DESIGN: the PayloadStore trait interface already exists (payload_store/mod.rs:95: put/get/delete_older_than/storage_type; impls filesystem/s3/gcs) — reuse it, don't reinvent. REQUIREMENT: refactor the backend selection to a PROPER DISCRIMINATED UNION — replace the flat `store_type: PayloadStoreType` + parallel always-present filesystem/gcs/s3 structs (config.rs:28) with a serde-tagged enum (internally tagged on "type") whose each variant carries ONLY its own backend config (Filesystem(FilesystemStoreConfig) | S3(S3StoreConfig) | Gcs(GcsStoreConfig)), so exactly one backend is selectable and invalid states are unrepresentable; adding a backend = adding a variant. init_payload_store matches the variant → Arc<dyn PayloadStore>. Proper polymorphism end-to-end: OffloadingEventBus is currently generic <S: PayloadStore> (static dispatch) which fights runtime config selection — make the selected store flow as Arc<dyn PayloadStore> (dynamic dispatch) instead of monomorphizing over a concrete type. ERGONOMIC: if only ONE backend variant is present/configured, use it without requiring the discriminator ("type") key — the tag is only mandatory to disambiguate when 2+ are present. (Apply this single-entry rule to every discriminated config union.) ✅PATTERN DECIDED: central internally-tagged serde enum (NOT the inventory registry) — lowers complexity by removing the registry indirection + parallel always-present structs + invalid-state handling (one enum + one match → Arc<dyn PayloadStore>). Multi-select cases use a list/array Vec<variant> (N/A here — single payload store). ✅TDD REQUIRED: tests first; for this cross-backend integration write Cucumber features + a shared harness exercising the offload→resolve round-trip against filesystem/S3/GCS (testcontainers).
- [ ] `washable-stipulate-casing` [HIGH][config] Bus/messaging config → central tagged enum. Replace MessagingConfig flat messaging_type + parallel amqp/kafka/ipc/nats/pubsub/sns_sqs structs (bus/config.rs) + inventory registry (bus/factory.rs) with one internally-tagged enum → central match → Arc<dyn EventBus>. Single-entry omits tag. TDD + cross-backend Cucumber harness. Pairs with subscribe-all (outspoken-next-common) + kafka dispatch path (docile-overnight-chant); also lets us drop the dead duplicate NatsBusConfig (juicy-favorable-system).
- [ ] `frosted-favored-wrinkle` [MED][config] DLQ config → tagged enum expressed as a LIST. Replace DlqTargetConfig flat dlq_type + parallel Option<...> structs with an internally-tagged enum per backend; model the priority chain as Vec<DlqBackend> (the multi-select case). Unify init_dlq_reader (currently a hardcoded match, dlq/factory.rs:132) onto the same enum. Resolve → Arc<dyn DeadLetterPublisher>/DeadLetterReader. Single-entry omits tag. TDD + cross-backend Cucumber harness.
- [ ] `pretended-mannish-aftermath` [MED][config] Service discovery → config-driven tagged enum. Replace the env-var-only, per-binary if/else selection (ANGZARR_DISCOVERY in bin/*) with a DiscoveryConfig internally-tagged enum (static | k8s | ...) in the YAML config, resolved → Arc<dyn ServiceDiscovery>, mirroring the already-compliant TransportType (transport/config.rs:25 — the reference example). Single-entry omits tag. TDD.
- [ ] `stubborn-trustable-blip` [MED][audit] Conformance review of ALL event-bus backends. Evaluate each bus impl (ipc, amqp, kafka, nats, pubsub, sns_sqs, mock) against current code needs/desires: is it still wanted/used in a real deployment profile; does it conform to the EventBus trait contract (incl. the subscribe-all contract once decided); is it carrying dead/duplicated logic; should it be kept, updated, or dropped. Output: per-backend keep/update/drop recommendation. Feeds the bus config registry/tagged-enum refactor (washable-stipulate-casing) — only keep backends in the union that survive this audit.
- [ ] `dreary-educated-chrome` [MED][audit] Conformance review of ALL storage components. Evaluate each event store / snapshot store / position store impl (postgres, sqlite, redis, bigtable, dynamo, nats, immudb, mock + shared SqlSnapshotStore/SqlPositionStore) against current needs/desires: still wanted/used; conforms to the EventStore/SnapshotStore/PositionStore trait contracts (incl. cascade methods); dead/duplicated logic; keep/update/drop per backend×role. Output: which backends populate each role registry. Feeds the storage registry refactor (immovable-undone-impulse) and overlaps the drop-immudb decision.
- [ ] `stainable-anguished-cuddle` [HIGH][immudb] KEEP ImmuDB — first-class, potentially the PRIMARY event store. Rework to integrate it cleanly (no more special-case stubs). DECISION: keep (NOT drop); the "event database" the user wanted gone was NATS (done). Plan: (1) dedicated ImmudbConfig (uri + verification/proof knobs) instead of reusing PostgresConfig; wire ImmuDB into the factory's central init_event_store match → Arc<dyn EventStore> (Instrumented-wrapped). (2) Under the split-selections registry (immovable-undone-impulse), express "events: immudb + snapshots: redis/sql + positions: sql" — the mix the legacy single `type` couldn't. (3) Implement cascade/2PC on ImmuDB (baffling-upturned-chaplain) — append-only INSERT+SELECT, compatible with immutability; remove the NotImplemented stubs. (4) Read-path: events are immutable & never compacted, so snapshots are LOAD-BEARING — require an ImmuDB-events deployment to pair a snapshot + position store and have the read path load snapshot+tail. (5) ✅DECISION (editions): ImmuDB is first-class EQUAL. Default/main chain is append-only in ImmuDB (tamper-proof); EDITIONS are frequently created AND deleted, but ImmuDB's logical DELETE never reclaims space → editions must NOT live in ImmuDB. Option B: pair ImmuDB with a SECOND, MUTABLE database for editions. ✅DESIGN (compose over uniform interface): the edition store implements the FULL EventStore trait (same as any primary), so build a generic `CompositeEventStore { main: Arc<dyn EventStore>, editions: Arc<dyn EventStore> }` that is ITSELF an EventStore (decorator like Instrumented/Offloading) — NOT immudb-specific; any (append-only main + mutable editions) pair composes. Routing by the `edition` arg: default chain → main (immudb, append-only, cascade-via-markers); non-default editions → editions store (mutable, delete_edition_events reclaims). Cross-store edition reconstruction = main[0..divergence] + edition[divergence..]; divergence already modeled (editions metadata table, schema.rs:90; sqlite already does this merge in-DB at event_store.rs:108,166 — lift it to the composite). Main-timeline guard (MAIN_TIMELINE_PROTECTED) still refuses deletes on the default chain. Registry fit: composite is a backend type whose config references two registry entries by name (backends:{immudb-main, edition-sql}; events:{use: composite, main: immudb-main, editions: edition-sql}). (6) Surface tamper-evidence/proof verification as a first-class capability (the reason to use it). (7) Add ImmuDB + the CompositeEventStore to the cross-backend behavioral Cucumber harness (EventStore + cascade + edition create/delete contracts, testcontainer). Reinforces baffling-upturned-chaplain (now REQUIRED, not optional) and feeds dreary-educated-chrome (immudb = confirmed keeper).
- [ ] `aghast-underarm-storable` [MED][immudb] ImmuDB EventStore does not implement get_with_divergence (uses the trait default → NotImplemented), so the shared contract test test_edition_explicit_divergence_new_branch fails the storage_immudb target (event_store_tests.rs:1208/macro). Pre-existing on feat/snapshot-temporal-wiring (independent of the Cover.ext work). immudb already implements implicit-divergence composite reads (query_edition_events + get_edition_min_sequence); the explicit-divergence path just needs the same composite stitch parameterized by an explicit divergence point. Fits under the immudb rework (stainable-anguished-cuddle) / editions composite design. Until implemented, storage_immudb is red even though test_cover_ext_round_trips and the rest pass.

## Done

- [x] `irritable-scant-zombie` [HIGH][drop] Remove NATS (JetStream) ENTIRELY — spans storage + bus + DLQ. Delete: src/storage/nats/ (event/snapshot/position stores), src/bus/nats/ (bus, consumer, otel), the NATS DLQ publisher (dlq/config.rs NatsDlqConfig + its publisher), the `nats` cargo feature + async-nats dep, NatsStorageConfig + NatsBusConfig, the Nats variants in the storage role enums + MessagingConfig.nats + dlq nats config, the storage_nats and bus_nats test targets, deploy/k8s/helm/nats/ Helm chart, and nats mentions in bin docs/config.example.yaml. Not in any prod deploy profile (k8s/gcp/aws use amqp/pubsub/sns-sqs). Build-green + tests after. SUBSUMES (now moot, archived): jubilant-unrented-twister (nats subscribe-all), related-lunar-problem (nats underflow), juicy-favorable-system (dead duplicate NatsBusConfig).
- [x] `untidy-unfixable-friction` [MED][drop] Remove the IPC (named-pipe) event bus — src/bus/ipc/. Delete the module + its inventory registration (bus/ipc/mod.rs:64), IpcBusConfig + MessagingConfig.ipc field (bus/config.rs), the #[cfg(unix)] ipc module in bus/mod.rs, and ipc mentions in docs/config. ⚠️ DO NOT touch src/transport/ UDS (type: uds) — that is the gRPC transport, a SEPARATE concern from the bus/ipc named-pipe bus. The default in-process bus stays `channel` (messaging_type default), so dropping IPC does NOT remove the in-process path. Build-green + tests after. SUBSUMES (now moot, archived): unstaffed-remote-excess (IPC oversized). The payload-offload wiring (jubilant-obsessive-cure) stays — it solves oversized generically across the remaining transports.

## Archived

- [x] `unstaffed-remote-excess` [CRIT] IPC bus: oversized (>10MB) message returns Reopen without draining the body from the FIFO → leftover bytes read as next length prefix → stream corruption (bus/ipc/client.rs:236). Add publisher-side size check (client.rs:610) and length-prefix overflow guard (client.rs:639); prefer fail-fast at publish with a shared MAX_MESSAGE_SIZE constant. ✅DECISION: defer oversized bodies to the existing claim-check / URI-to-body pattern (OffloadingEventBus + PayloadReference, bus/offloading.rs; wrap_with_offloading at factory.rs:65; EventPage.payload oneof event|external). NOTE the real gap: that infra is NOT wired into the production binaries — init_payload_store()/wrap_with_offloading() are never called by angzarr_aggregate/saga/process_manager/projector. Real fix = wire it into binary startup + enable payload_offload config (solves oversized across ALL transports). IPC is dev-only (live but no prod Helm profile uses it), so don't build IPC-specific inline handling; just keep a fail-fast guard for when offload is disabled. (Consider splitting "wire payload-offload into binaries" into its own task.)
- [x] `juicy-favorable-system` [HIGH][cleanup] Delete dead duplicate NatsBusConfig in bus/config.rs:196 — incompatible API vs nats/config.rs, never instantiated; confuses architecture and violates in-proc/distributed parity. (prototype: delete, don't keep.)
- [x] `related-lunar-problem` [HIGH] NATS: delete_edition_events computes (count_before - count_after) as u32 — concurrent additions underflow to a huge count (storage/nats/event_store.rs:1076). Use saturating_sub.
- [x] `jubilant-unrented-twister` [CRIT] Fix subscribe-all in NATS bus: SubscriberAll sets domain_filter=None but start_consuming requires Some → runtime Subscribe error/crash (bus/nats/bus.rs:224, mod.rs:89). Conform to the subscribe-all contract task. ✅DECISION: require explicit domains (from env) and reject empty/None at init with a clear error — do not silently accept None.

