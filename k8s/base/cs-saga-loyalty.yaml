apiVersion: apps/v1
kind: Deployment
metadata:
  name: cs-saga-loyalty
  namespace: angzarr
  labels:
    app: cs-saga-loyalty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cs-saga-loyalty
  template:
    metadata:
      labels:
        app: cs-saga-loyalty
    spec:
      containers:
        - name: cs-saga-loyalty
          image: cs-saga-loyalty:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 50602
          env:
            - name: PORT
              value: "50602"
          readinessProbe:
            grpc:
              port: 50602
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            grpc:
              port: 50602
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cs-saga-loyalty
  namespace: angzarr
spec:
  selector:
    app: cs-saga-loyalty
  ports:
    - port: 50602
      targetPort: 50602
