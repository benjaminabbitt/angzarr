# Tasks

## In Progress

## To Do

## Backlog

### Mutation Testing - Needs Improvement
- [ ] **improve-orchestration-aggregate**: `orchestration/aggregate/mod.rs` - 39% kill rate. Add tests for DLQ, fact idempotency, gRPC business logic paths.
- [ ] **improve-bus-outbox**: `bus/outbox/mod.rs` - 50% kill rate. Add tests for recovery task, orphan detection, create_subscriber.
- [ ] **improve-bus-ipc**: `bus/ipc/client.rs` - 41% kill rate. Many timeouts due to async IPC. Add tests for pipe reading, domain filtering.
- [ ] **improve-orchestration-local**: `orchestration/aggregate/local/mod.rs` - 45% kill rate. Add tests for context factory, execute_aggregate, and sequencing.
- [ ] **improve-standalone**: `standalone/server.rs`, `client.rs`, `builder.rs` - 11% kill rate. Critical - needs comprehensive test suite.

### Other Backlog
- [ ] Unify output system interface — abstract consistent interface between buses and gRPC for command/event emission
- [ ] Validate monitoring/otel, ensure that everything is visible as designed
- [ ] Look for potential memory leaks or other runtime maladies
- [ ] Validate svg generation for architecture out of opentofu and otel
- [ ] On PR create, run tests, linting, and formatting
- [ ] Code review for consistency — patterns across PMs, Aggregates, Projectors, and Sagas should be similar; clients should be consistent across languages
- [ ] Set up remote debugging on containers
- [ ] Angzarr CLI for DLQ replay, events, replay

## Reminders

- Each example self-contained: symlinks to shared features/, own justfile, only depends on angzarr framework

## Notes

### Architecture Observations
- Storage tests use macro pattern (~108 LOC/backend with 703 LOC shared macros)
- Bus tests use similar macro pattern (~120 LOC/backend with 368 LOC shared)
- Key reference: `tests/storage/event_store_tests.rs`, `tests/bus/event_bus_tests.rs`

### Implementation Notes
- Bigtable/DynamoDB use StorageError::NotImplemented for errors — naming is intentional (errors, not missing features)
- immudb delete returns NotImplemented by design — immutable storage
- IPC/Channel skip trace context propagation — standalone mode, context flows naturally within process
- metrics crate IS used in advice/instrumented.rs for storage metrics wrapper
