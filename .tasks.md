# Tasks - Tasks

## In Progress

## To Do

## Backlog
- [ ] Implement OpenAPI sidecar proxy for grpc, rest proxy
- [ ] Unify PM retry logic to use RetryableOperation trait like Saga — orchestrate_pm has inline retry loop while orchestrate_saga uses well-designed RetryableOperation abstraction
- [ ] Add AggregateContextFactory trait — Saga has SagaContextFactory, PM has PMContextFactory, but Aggregate has no factory (inconsistent pattern)
- [ ] Complete or remove temporal query implementation — scattered TODOs in event_query/mod.rs:132, standalone/client.rs:298,324,367, projector_coord.rs:195 suggest half-implemented feature
- [ ] Decide on single testing paradigm — codebase now has 2 styles: macro-based (storage, bus) and Gherkin (interfaces). Consider consolidating
- [ ] Evaluate advice/ module — feature-gated with no internal usage, may be speculative infrastructure
- [ ] Remove or use checkpoint monitoring stubs — src/bus/ipc/checkpoint/mod.rs:223 stats() and CheckpointStats marked dead_code
- [ ] Implement Selection::Sequences in event_query — src/services/event_query/mod.rs:132,298 falls back to full query instead of specific sequence fetching
- [ ] Implement payload resolution in OffloadingEventBus — src/bus/offloading.rs:213 subscribe() is pass-through only
- [ ] Add domain routing by handler descriptors — src/standalone/speculative.rs:122,163,226 TODOs for smarter routing vs first-registered fallback
- [ ] Implement Go OO aggregate revocation handling — client/go/aggregate_oo.go:474 returns DelegateToFramework stub
- [ ] Allow Rust handlers to declare destinations — client/rust/src/handler.rs:159 TODO for saga destination declaration
- [ ] Use proto_reflect for state diffing — src/orchestration/aggregate/mod.rs:360 uses JSON comparison workaround
- [ ] Add MergeStrategy::MergeManual to proto — src/dlq/mod.rs:37 TODO, enum value referenced but not in proto
- [ ] Java step definitions don't match shared features — examples/java/tests/build.gradle.kts:37 tests compile but scenarios don't run
- [ ] Python Gherkin steps are simplified stubs — examples/python/features/steps/compensation_steps.py has 30+ `pass` placeholders instead of real verification
- [ ] validate monitoring, look for potential memory leaks or other runtime maladies
- [ ] validate svg generation for architecture out of opentofu and otel
 
## Done

- [x] Refactor bus consumer functions — extracted helper functions for message processing, domain filtering, checkpoint handling. SNS/SQS ~190→~45 lines, IPC ~158→~40 lines, NATS ~123→~45 lines, PubSub refactored.
- [x] Fix PubSub API incompatibility — switched from google-cloud-pubsub (donated to Google, different API) to gcloud-pubsub (yoshidan's crate, same API)
- [x] Fix GCS API incompatibility — switched from google-cloud-storage (donated to Google, different API) to gcloud-storage (yoshidan's crate, same API)
- [x] Remove immudb from interface tests — immudb only supports EventStore (immutable by design), not SnapshotStore/PositionStore. Interface tests require all three. Test immudb via tests/storage_immudb.rs instead.
- [x] ImmuDB EventStore implementation
- [x] PostgresEventStore implementation
- [x] RedisEventStore (MemoryDB) using sorted sets for events
- [x] BigtableEventStore for GCP
- [x] Research optimal event-sourcing storage on AWS (Aurora vs DynamoDB vs Timestream vs QLDB) -> MemoryDB recommended
- [x] Research optimal event-sourcing storage on GCP (Cloud Spanner vs Cloud SQL vs Firestore vs Bigtable) -> Bigtable recommended
- [x] Fix SQLite schema: composite PK (domain, root, sequence), removed surrogate id
- [x] Fix SQL injection in SqliteSnapshotStore::put - now uses sea-query OnConflict
- [x] Remove unused hex dependency
- [x] Build Helm charts for k8s deployment (deploy/helm/)
- [x] Rust example: integration tests running
- [x] Go example: integration tests running
- [x] Python example: integration tests running
- [x] Java example: integration tests running
- [x] C++ example: integration tests running
- [x] C# example: integration tests running
- [x] Build dev container for Rust examples
- [x] Build dev container for Go examples
- [x] Build dev container for Python examples
- [x] Build dev container for Java examples
- [x] Build dev container for C++ examples
- [x] Build dev container for C# examples
- [x] Extract edition stamping to shared utility — duplicated 8 times (~60 LOC) in saga/pm local+grpc modules (orchestration/saga/local/mod.rs:50,77, orchestration/saga/grpc/mod.rs:80,113, orchestration/process_manager/local/mod.rs:59,85, orchestration/process_manager/grpc/mod.rs:75,121)
- [x] Delete or implement StaleProcessQuery trait — src/services/timeout_scheduler.rs has 200+ LOC of unused infrastructure with zero implementations
- [x] Implement or remove saga compensation stubs — send_to_dead_letter_queue() and trigger_escalation() in src/utils/saga_compensation/mod.rs:358,387 log but don't actually send
- [x] Extract domain lookup helper — `clients.get(domain).ok_or_else(|| Status::not_found(...))` pattern duplicated 4+ times in router.rs, speculative.rs, destination/grpc, command/grpc
- [x] Extract UUID root extraction to Cover trait extension — `.root.as_ref().and_then(|r| Uuid::from_slice(&r.value).ok())` duplicated 8+ times across orchestration/ and services/
- [x] Extract error boxing helper — `.map_err(|e| Box::new(e) as Box<dyn std::error::Error + Send + Sync>)` duplicated 9 times across standalone/client.rs, standalone/router.rs, orchestration/saga/*.rs
- [x] Refactor angzarr_standalone main() — src/bin/angzarr_standalone.rs:290 (424 lines). Extract storage initialization, process spawning, and endpoint binding into separate functions
- [x] Refactor format_event() — examples/rust/prj-output/src/main.rs:89 (95 lines) large match statement could use trait-based dispatch or HashMap lookup
- [x] const-ize all constant strings
- [x] Extract shared CapturingHandler to test utility — moved to src/test_utils.rs with test-utils feature flag. Added with_storage() and with_tracker() methods for BDD test patterns. Updated 5 bus tests + interface tests.
- [x] angzarr aggregate documentation — updated to show all storage backends (postgres, sqlite, redis, bigtable, dynamodb) and messaging options (amqp, kafka, pubsub, sns-sqs, nats, ipc, channel)
- [x] Look for more potential library code in the examples — identified saga helpers, state_from_event_book patterns, test fixtures as candidates (see new tasks)
- [x] Unify bus tests with macro pattern like storage tests — created tests/bus/ with event_bus_tests.rs (368 LOC shared) + run_event_bus_tests! macro. Reduced duplication from ~1966 LOC to ~1186 LOC. Each backend now ~110 LOC vs ~393 LOC before.
- [x] Terraform for AWS: EKS + MemoryDB for Redis (event store + cache)
- [x] Terraform for GCP: GKE + Bigtable (event store + cache)
- [x] Python client: Add SagaHelper with destination_map() and next_sequence_for() utilities
- [x] Refactor get_by_correlation() — src/storage/nats/event_store.rs:490 (119 lines) has multiple nested iteration levels
- [x] Python examples: Standardize state_from_event_book pattern across aggregates (player, hand, table)
- [x] Python examples: Update tests to use angzarr_client.testing instead of local conftest fixtures
- [x] Go through Rust codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how
- [x] Go through Rust codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how.   Look for areas of higher complexity that could use more docs.
- [x] Go through Go codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how. Look for areas of higher complexity that could use more docs.
- [x] Go through Python codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how. Look for areas of higher complexity that could use more docs.
- [x] Go through Java codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how. Look for areas of higher complexity that could use more docs.
- [x] Go through c# codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how. Look for areas of higher complexity that could use more docs.
- [x] Go through C++ codebase and ensure that all comments are up to date, and that all areas of complexity has comments talking about the *why*, the code is the how. Look for areas of higher complexity that could use more docs.
- [x] Another pass through the docusaurus documentation, we'll want to compile large chunks of it from source code comments
- [x] Fix missing Go doc files/markers: prj-cloudevents/main.go, pmg-hand-flow/main.go, prj-output/output_projector_doc.go, saga-hand/splitter_example.go, tests/player_test.go
- [x] Remove redis as an event store

## Reminders

- Each example self-contained: symlinks to shared features/, own justfile, only depends on angzarr framework

## Notes

- ROOT CAUSE: No periodic consolidation — features added without refactoring similar existing code
- ROOT CAUSE: Different styles across modules — storage vs bus tests likely written at different times/by different contributors
- ROOT CAUSE: Speculative infrastructure — StaleProcessQuery, advice/ module built before actual need
- METRICS: Storage tests avg ~108 LOC/backend with 703 LOC shared macros
- METRICS: Bus tests avg ~120 LOC/backend with 368 LOC shared code (unified with storage pattern)
- METRICS: Bus tests total ~1,186 LOC vs Storage tests ~540 LOC for similar functionality (was 1,966 LOC before)
- KEY FILES: tests/storage/event_store_tests.rs (703 LOC) — the good pattern
- KEY FILES: tests/bus/event_bus_tests.rs (368 LOC) — now also uses macro pattern
- Dev containers include: proto bindings, test deps, mount source code, run tests via justfile
- Examples present: cpp, csharp, go, java, python, rust (6 total)
- Long functions: standalone main() 424 LOC, bus consumers 120-190 LOC each
- Code duplication: edition stamping (8x), error boxing (9x), UUID extraction (8x), domain lookup (4x)
- Stub implementations: saga compensation DLQ/webhook, Go OO revocation, Python Gherkin steps
- Dead code: Runtime::limits, StaticServiceDiscovery::namespace, advice/ module (feature-gated, unused)
- Bigtable/DynamoDB use StorageError::NotImplemented for errors — naming is confusing but intentional (errors, not missing features)
- immudb delete returns NotImplemented by design — immutable storage, this is correct
