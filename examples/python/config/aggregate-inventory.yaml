# Inventory aggregate sidecar configuration (standalone mode)
storage:
  type: sqlite

transport:
  type: uds
  uds:
    base_path: /tmp/angzarr

target:
  address: /tmp/angzarr/business-inventory.sock
  domain: inventory
  command: ["uv", "run", "--directory", "inventory", "python", "server.py"]
