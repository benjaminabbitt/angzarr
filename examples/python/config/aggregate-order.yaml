# Order aggregate sidecar configuration (embedded mode)
storage:
  type: sqlite

transport:
  type: uds
  uds:
    base_path: /tmp/angzarr

target:
  address: /tmp/angzarr/business-order.sock
  domain: order
  command: uv run --directory order python server.py
