# Customer aggregate sidecar configuration (standalone mode)
storage:
  type: sqlite

transport:
  type: uds
  uds:
    base_path: /tmp/angzarr

target:
  address: /tmp/angzarr/business-customer.sock
  domain: customer
  command: ["uv", "run", "--directory", "customer", "python", "server.py"]
