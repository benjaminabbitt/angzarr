[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Build automation tasks for angzarr"
license = "AGPL-3.0-only"
publish = false

[dependencies]
syn = { version = "2", features = ["full", "parsing", "visit"] }
glob = "0.3"
toml_edit = "0.22"
